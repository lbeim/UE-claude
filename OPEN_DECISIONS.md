# OPEN DECISIONS — UE-Claude

Vorwärts-Backlog: **nur offene Items**. Erledigtes lebt im git log (`git log --oneline`), nicht hier. Status: `[PENDING]` / `[VERIFY-OPEN]`.

---

## Operator-Pending

- **D-1 Pulse-Trial-Gaps** `[VERIFY-OPEN]` — Material-Authoring-Trial mit Epic MaterialTools fahren, dann die 5 Hard-Gaps + 3 Schnittstellen-Findings (Pulse-Trial 2026-05-12) gegen den aktuellen Epic-Plugin-Stand re-evaluieren, bevor sie als Memory/Doc landen. Kriterium: allgemeine Relevanz UND echte Friction. Quelle: `rbga-smart-core` Memory `project_material_authoring_vision`.
- **D-3 Inline-STOP** `[PENDING]` — Soft-STOP ("STOP/halt/warte" im Text) zusätzlich zu Ctrl+C aktivieren? Aktuell deaktiviert. Mit dem CLAUDE.md-Abbau ersatzlos schließbar, falls kein Bedarf.
- **D-12 OSCBridge Editor-Mode-Cycle-Noise** `[PENDING]` — Receiver durchläuft Stop/Destroy/Start-Cycles bei Map-Load + `SE_EADDRINUSE` nach PIE-End. Funktional ok (Stream läuft), nur Log-Noise. Fix wenn's nervt: World-Type-Filter strenger / `SE_EADDRINUSE`-Retry mit Backoff / `OnConstruction`-Re-Entry-Guard.
- **D-17 PulseKit Wave-Data-Bridge + per-Welle-Energie→Speed** `[PENDING]` — Entkoppelte, generische „Wellen-Attribut"-Bridge (`UWaveDataBridge`: Center/Age/Energy + erweiterbare Wildcard-Map, bei Geburt gelatcht) über **MID-Param-Transport** (Operator-Entscheid 2026-06-03; CPD raus = 36-Float-Cap < ~60 nötig [[reference-ue-custom-data-to-material-transports]]); PulseField/Trigger → reiner Spawn-Scheduler, füttert via Intake `SubmitWave(center, {attrs})`. Feature: per-Welle-Energie (Geburts-Latch) → eigener Speed + globaler „Brake"-Skalar (über Zeit, Verhältnisse erhaltend). **Phase 1** (shader-first, Code-Loop, kein C++): `SpeedScale`+`SpeedBrake`-Knöpfe + Energie→Speed-Mathe + Platzhalter-Energie (PHN-Hash) in HLSL, Feel iterieren. → **GEBAUT+GRÜN 2026-06-03**: 2 Params (SpeedScale 0.5 / SpeedBrake 1.0, Gruppe „1 Pulse - Form" sort 6/7), Custom-Node 46→48 Inputs (Full-Struct-Append, Reader-verifiziert), HLSL `radius=Speed*(1+SpeedScale*energy)*SpeedBrake*age` (energy=`frac(sin((PHN+1.0)*78.233)*…)` Platzhalter), Deploy grün+gespeichert, Contract neu. **Offen: Operator-Eye-Test + Live-Tune (Kurve/Werte/Smoothness); Git-Commit 2026-06-03 als Checkpoint gesetzt (Eye-Test weiter offen).** **Phase 2 (additiv) GEBAUT+GRÜN 2026-06-03**: echte per-Slot-Energie — 12 `PulseEnergy0..11`-Pins (60 Inputs), HLSL `energy=(PulseEnergy<N>)`, C++ `PulseTriggerComponent` sampelt `EnergyParameter` (Default `sub_pressure`) bei FirePulse → latch `SlotEnergy[]` → schreibt `PulseEnergy%d` (mirror PulseAge). Build grün (Worker-Review PASS; ⚠️ bridge_rebuild build_rc=6 war SPURIOUS — Log „Result: Succeeded", CDO `EnergyParameter=sub_pressure` verifiziert), Material+Contract+Memory aktualisiert. **Offen: Operator-Eye-Test.** **Bewusst VERSCHOBEN** (Worker-Risiko-Scan): volle generische `UWaveDataBridge` + PulseTrigger→Scheduler-Split (verdoppelt BP_PulseField-CS-Fragilität für 0 sichtbaren Mehrwert) + Brake-Akkumulator (Lurch im Eye-Test BESTÄTIGT → eigener nächster Schritt, s.u.). Git-Commit 2026-06-03 als Checkpoint gesetzt (Eye-Test weiter offen). Detail [[project-pulsekit-radial-pulse]].
  - **Brake-Akkumulator gegen Ring-Retraction: GEBAUT+GRÜN 2026-06-03, Operator-Eye-Test offen.** Fix für: Live-Drehen an SpeedBrake/SpeedScale saugte fliegende Ringe nach innen (`radius=spd×age` memoryless). Tempo in die C++-Zeit-Integration gefaltet, **reuse PulseAge = effective age, KEINE neuen Pins**. **HLSL v0.4** (`Shaders/RadialRipples.hlsl`): `RIPPLE`-Macro → `radius = Speed * age` (energy/spd-Zeilen raus; SpeedScale/SpeedBrake/PulseEnergy radius-seitig unused, Pins bleiben). **C++ `DoTick`** (.cpp-only): liest `SpeedScale`/`SpeedBrake` live aus MID (ParameterName Reader-verifiziert) → `factor=max((1+SpeedScale*SlotEnergy[i])*SpeedBrake, 0.02)`, `SlotAge[i]+=factor*DeltaTime`; Retire `effAge>=Bounds/Speed` → `radius=Bounds` am Rand, rate-unabhängig. Deploy grün (readback byte-match, 60 Inputs/Roots heil, error-Scan leer); Build grün (`_rebuild_build.log` „Result: Succeeded" + PulseTriggerComponent.cpp compiled+linked; ⚠️ `bridge_rebuild build_rc=6` SPURIOUS — laufender Broker noch vor build-verdict-Härtung, Verdikt via Log). Contract v0.4 + Memory aktualisiert. **Offen: Operator-Eye-Test in LED_Ndisplay** (Realtime + ALT+P→ESC; Drehen ändert nur die RATE, kein Zurückwandern?). Slate-CVar in `Config/DefaultEngine.ini` → Viewport bleibt nach Rebuild live [[reference-ue-slate-throttling-viewport-freeze]]. Git-Commit 2026-06-03 als Checkpoint gesetzt (Eye-Test weiter offen).
  - **non-uniform Brake (`BrakeBias`): GEBAUT+GRÜN 2026-06-03, Operator-Eye-Test offen.** Operator-Approval Akkumulator („break is cool") → direkt gebaut. Material 60→61 Inputs (`BrakeBias` Pin 60 = `ScalarParameter_52`, Group „1 Pulse - Form" Sort 8, Default 0; Full-Struct-Append, 60 Alt-Kanten + 3 Roots Reader-verifiziert byte-erhalten). C++ `DoTick`: `WaveBrake=lerp(SpeedBrake,1,clamp(SlotEnergy[i]*BrakeBias,0,1))`, `factor=max((1+SpeedScale*energy)*WaveBrake,0.02)`. 0=uniform (=Akkumulator), hoch=energievolle Wellen widerstehen dem Bremsen → Rudel spreizt. Build grün (Log „Result: Succeeded"; 61 Inputs+BrakeBias nach Reload verifiziert, shader-error-Scan leer). Contract+Memory aktualisiert. **Offen: Operator-Eye-Test** (BrakeBias live hochdrehen beim Bremsen). **Operator-OK 2026-06-03** (Clamps nur gegen Artefakte — sonst sind extreme Werte/Umkehrungen erwünscht [[feedback-shader-tuning-surface]]): BrakeBias-Clamp `clamp(SlotEnergy*BrakeBias,0,1)` RAUS beim **nächsten** PulseKit-C++-Build (kein Extra-Rebuild jetzt, bei BrakeBias=0 identisch; Factor-Min-Clamp 0.02 BLEIBT = frozen-slot-Schutz). Git-Commit 2026-06-03 als Checkpoint gesetzt (Eye-Test weiter offen).
  - **Generische Channel-Selector-Engine: GEBAUT+GRÜN+VERIFIZIERT 2026-06-03 (`a6fac71`), nur Operator-Eye-Test offen.** Operator-Pivot: fixe Trigger/Energy-FName-Logik raus → **alle MPC-Kanäle per Index live aus Material-Scalars wählbar, kein Rebuild mehr fürs Iterieren an Kanal-Wahl/Operation/Modi**. C++ `GetScalarParameterNames()`-Index-Zugriff; `DoTick` TriggerChannel/TriggerThreshold/TriggerMode (0 Rising/1 Falling/2 Level/3 Wrap), `FirePulse` EnergyChannelA/B(−1=aus)/Op(+−×÷, ÷ div-guard)/Scale/Bias → `SlotEnergy=(A op B)*Scale+Bias`. **D-17c erledigt** (WaveBrake-Lerp-Upper-Clamp raus, high-Energy überschiesst base-Speed; Min 0.02 bleibt). 8 freie ScalarParameter „0 Pulse - Engine" (unverbunden=MID-lesbar, gegen UE-5.8-Source Subagent-verifiziert), Custom-Node unverändert 61 Inputs. **Durable-Win**: Config lebt in Material-Scalar-Defaults → überlebt Editor-Neustart+CS-Recreate (altes Instanz-Override-Verlust-Problem WEG, post-restart Readout verifiziert: `phase [Rising @ 0.70]` / `(LOW+BASS)*1.00`). Operator hat parallel significance/state_pulse aus Engine-Stream+Router entfernt → MPC = exakt die 13 `07_CHANNEL_REFERENCE.md`-Kanäle. Detail [[project-pulsekit-radial-pulse]] + `Shaders/RadialRipples.contract.md`. **Eye-Test-Punkte:** BrakeBias-Spreizung, LOW+BASS vs nur-LOW vs ×, Trigger-Modi Rising vs Wrap.

## Verify-Open — Code grün, Operator-Eye-Test offen

- **D-18 PulseKit Phase-3 SwellFront WPO** (2026-06-03, grün+gespeichert) `[VERIFY-OPEN]` — Echtes 3D-Geometrie-Rippling: `M_RadialPulse` out2 (WPO) = 12 radiale Gerstner-Wellenfronten (SwellFront) statt `float3(total)`, + analytische Normale out4 → MP_Normal. Custom-Node **105→125 Inputs** (19 Scalars + VectorParameter BorderCenter, Gruppen „7 Pulse - WPO Form/Amplitude/Conductor/Border"), neuer AdditionalOutput out4 `WaveNormal` (Float3). Graph-Chirurgie (mit Op, tangent-space): WorldPos→ExcludeOffsets, out2→MP_WorldPositionOffset direkt (alte ×VertexNormal ×PulseWPOAmount verwaist), out4→Transform(World→Tangent)→MP_Normal. Compile grün (6× clean recompile, einziger Fail @ 21:19:39 = erster Broken-Deploy, danach durchgehend grün) + save_assets. Spec KANONISCH `docs/PHASE3_WPO_DESIGN_SwellFront.md`; Build `Tools/shader-deploy/deploy_v10_swellfront.py` + Lint `_lint_hlsl.py`; Contract `Shaders/RadialRipples.contract.md` v1.0-WPO. **Offen: Operator-Eye-Test** (6 Presets durchprobieren; BorderCenter auf Mesh-Mitte setzen; WaveMaster/Amplitude/Crisp/ConstructiveGain/Conductor-Kopplung tunen; nDisplay-Normal-Render-Pfad validieren) **+ optional Aufräumen der 4 verwaisten Alt-WPO-Nodes** (Op-approved Delete). Detail [[project-pulsekit-radial-pulse]]. (Lesson: mehrzeilige Block-Kommentare im `#define`-Macro fressen das `\` → Macro-Trunkierung; jetzt einzeilig + Lint-Check.)
- **D-13 PulseKit-Tuning** (`f2ef18b` + 2026-06-02-Rebuild, grün, noch nicht committed/eye-getestet) `[VERIFY-OPEN]` — Offen: (a) Effekt **viel dezenter** (Wand ~20 m, darf nicht überwältigen); (b) **Spline über die GANZE Wand** (X −650…+650, Z 100…605; aktuell links-mittig); (c) **significance scharf** (CS erzwingt noch `bAutoFirePreview=true`); (d) später Licht + Muster. Detail [[project-pulsekit-radial-pulse]]. (Shader-Teil → D-16.)
- **D-14 Bridge-Worker Adoption** (`d5f92e6`) — `start-bridge.cmd` dauerhaft laufen lassen + Claude einmal neu → Robustheit dauerhaft live. Optional: Boot-Autostart, SSE-Live-Progress.
- **D-16 Shader-Refactor + Graph-Reader** (2026-06-02, grün+gespeichert) `[VERIFY-OPEN]` — M_RadialPulse Custom-Node = 3 Outputs (Emissive/WPO/BaseColor, WPO entkoppelt), 12 tote Nodes raus (69→57); eigenes Graph-Reader-Toolset gebaut [[reference-mcp-material-connection-read-gap]]. **Session 3 (2026-06-02) erledigt, grün+gespeichert:** Helligkeits-Boden (Floor/Clip auf Emissive) · Rotations-Regler (Spin/SpinLobes als Live-Params) · Param-Gruppen (9 chaotische → 6 saubere „Pulse - *" + Description je Param, C++-Writes per Subagent verifiziert). Offen: BaseColor aktivieren — `out1 × BaseColorScale → Albedo` ist verdrahtet, default 0 = aus (NICHT Operator-Color, die sitzt im Emissive); = BaseColorScale hochdrehen ODER out1→Roughness/Specular umhängen (Operator-Entscheid). **Infra 2026-06-02:** HLSL jetzt versionierte Source-of-Truth `Shaders/RadialRipples.hlsl` (+`.contract.md`) + Deploy-Pipe `Tools/shader-deploy/deploy_radialripples.py` (Datei→Code→recompile→Reader+Log-Verify) [[reference-shader-code-loop-pipe]]; erster Code-Loop = 7 Ripple-Blöcke → `#define RIPPLE` (9070→2210 Z., verhaltens-identisch, frischer Translate grün+gespeichert). **Offen-Konvention: Drei-Loop-Trennung (Code/Graph/Eye-Test) in CLAUDE.md baken (Operator-Nod).** Detail [[project-pulsekit-radial-pulse]], [[feedback-direct-fast-shader-iteration]]. (Color/Atlas = Operator-Domäne.)
- **D-15 OSC-Gradient-Atlas** (`be51989` + 2026-06-02 Persistenz/Naming) `[VERIFY-OPEN]` — §A+§B grün; Modell B + self-healing Persistenz (überlebt Crash, „auto-repainted 12 rows" verifiziert) + 12 Demo-MIs benannt (`MI_Atlas_<col>_Solid|Grad`). Offen: C++-Button „Generate State Instances"; §C Niagara `DataInterfaceTexture` / MaterialFunction + Eye-Test. Detail [[reference-oscbridge-atlas-persistence]]. (C++/Renames noch nicht committed.)

## RBGA — RC Param-Surface (neue Workstream, separat von PulseKit)

- **RBGA-U1 RC Param-Surface** `[U1 GEBAUT+VERIFIZIERT 2026-06-03 — Forward-Items offen]` — `BP_RBGAManager` (`/Game/RBGA/`, 1 Instanz in `Main`) exposed M_RadialPulse-Look-Params via **Remote Control** in den Dashboard-Preset `/Game/RemoteControl/RBGA_ReactRemoteControl` (editor-only, kein PIE, kein MPC). RC-Plugin war OFF → in `MCP.uproject` enabled (bridge_rebuild). Exposed Grundset: `Floor, PulseGain, BassGain, SparkleGain, Spin, WaveMode`. RC→MID→Shader visuell verifiziert (Floor 0.8↔0.0). Recipe/Gotchas → Memory [[project-rbga-rc-param-surface]]. **Forward:** (a) finaler/kuratierter Param-Satz + **C++-Migration** (`ARBGAManager`-Base + BP-Subclass) wenn der Satz steht (BP-Setter-pro-Param skaliert nicht auf ~30); (b) Atlas-Farb-Params (`Brightness/StateRow/SpeedX/Y`) bewusst draussen = Operator-Farb-Domäne; (c) **State-Crossfade nur bestätigen dass er glidet** (bestehende Mechanik, nicht gebaut); (d) nDisplay-Cluster später. react-dashboard NICHT anfassen.

## Backlog — unscheduled

- OSCBridge: Session Record/Replay, Data-Asset-basierte Binding-Tables, zeit-korrektes (DeltaTime-)Smoothing. (Reste aus D-6/D-7.)

---

*Erledigte Decisions → git log. Anker: D-0 Epic-MCP-Pivot (`6cf84c5`) · D-5 OSCBridge-Plugin (`d1e0444`) · D-6 Phase-2 (`22c7b63`) · D-7 Phase-3 (`de0283c`) · D-8 Hz/Jitter (`2f42fc5`) · D-9/D-10/D-11 Editor-Mode+Tags grün (`7690810`) · D-13 (`f2ef18b`) · D-14 (`d5f92e6`) · D-15 (`be51989`).*
