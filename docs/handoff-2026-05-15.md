# Handoff — OSCBridge Session 2026-05-15

Stand am Session-Ende. Bezug: OSCBridge-Plugin in UE 5.8 (MCP-Projekt).

## Was diese Session gebaut wurde

**Hz-Fix + Jitter-Spalte (D-8, am 2026-05-14):** Receiver-Hot-Path entschlackt (`HzEMA` raus), Inspector rechnet Fenster-Zaehlung + Std-Abweichung pro Refresh — stabile Hz, neue Jitter-ms-Spalte. Files: `OSCBridgeReceiver.h/.cpp`, `SOSCInspectorPanel.h/.cpp`.

**Editor-Mode OSC→MPC zuverlaessig (D-9):** Router-Editor-Subscription-Watchdog (`ShouldTickIfViewportsOnly` + `Tick` mit idempotentem `RebindReceiver`). Hatte vorher keinen Retry, Subscription kam nie zustande wenn Receiver erst nach Platzierung des Routers aktiv wurde. Engine-Seite (Editor-World-MPC-Propagation) gegen UE-5.8-Sources verifiziert — sauber, kein Engine-Bug. **Pflicht beim Testen: Viewport auf Realtime.** Files: `OSCBridgeRouter.h/.cpp`.

**Tag-Taxonomie + Tag-Getter + Wizard-Prefill (D-10):**
- Taxonomie-Decision: `<Zweck>.<Leaf>` (z.B. `Lighting.Brightness`). `OSC.`-Prefix gestrichen. Quelle am Receiver, nicht im Tag.
- 6 BlueprintCallable Tag-Getter am Router (`GetLatestFloat/Int/Vec3/Vec4/Bool/String(FGameplayTag)`, Kategorie `OSC|Values`). Pull-Pattern fuer BP-Werte-Konsum statt Event-Trichter.
- Wizard `AddressToTagName` neu: `/mod/intensity` → `Mod.Intensity` PascalCase-Platzhalter, Operator benennt leading Segment auf Zweck um.
- Files: `OSCBridgeRouter.h/.cpp`, `SOSCLearnWizard.cpp`.

Alle drei Bloecke kompilieren gruen gegen UE 5.8.

## Drei-Spuren-Modell (Mental-Anker fuer Plugin-Benutzung)

Aus D-10 / Session-Dialog destilliert:
- **MPC** → Werte die Material treiben (kein BP noetig)
- **Tag-Getter** → Werte fuer BP-Logik (zentrale, ueberall-verfuegbare Stelle, beliebig viele BPs ziehen)
- **`OnFloat`/`OnBang`-Events** → nur fuer reaktive Moment-Logik wo der Ankunftszeitpunkt zaehlt

## Offen — Operator-seitig

**Gebuendelter Eye-Test ausstehend:** Hz/Jitter im Inspector, Editor-MPC-Watchdog (Material auf Mesh reagiert ohne Play, Realtime-Viewport!), Wizard-Prefill, Tag-Getter im BP. Schritte: siehe Session-Transkript Ende 2026-05-15.

**Zweck-Kategorien:** mit Zweck-Root sind Kategorien Show-spezifisch. Wachsen ueber Wizard-Nutzung (Operator benennt leading Segment beim Anlegen um) — kein generischer Plugin-Default moeglich/sinnvoll.

**MPC-Auto-Create:** angenommen, vertagt — braucht HOW-Decision (wo es einhakt). MPC-Muedigkeit signalisiert, separat machen.

## Companion-Repo Hygiene

- `CLAUDE.md` hat uncommittete Modifikationen aus Vorrunden — nicht von dieser Session, Operator-Entscheid.
- `docs/handover-learn-mode-wizard.md` + `docs/orphan-asset-detection.md` untracked, in D-7 referenziert — koennten irgendwann mit rein.

## Pointer

- Tracker: `OPEN_DECISIONS.md` D-5 bis D-10.
- Plugin: `C:\development\Projects\MCP\Plugins\OSCBridge\` (Plastic SCM, separat).
- Neue Memories: [[reference-ue-editor-mpc-propagation]], [[feedback-static-read-isnt-runtime-verify]].
